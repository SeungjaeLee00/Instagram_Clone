# instagram clone 
지수영

10/8 (화) <br>
- 로그인 기능 
`-` index.js 
    1. app.post API => '/auth/login' <br><br>

`-` User.js
    1. comparePassword() => 비밀번호 일치 여부 확인하는 함수
    2. generateToken() => 토큰 생성 함수